# Copy to .env and adjust if you changed the compose ports.
DATABASE_URL=postgresql://since:since_dev_password@localhost:5433/since
REDIS_URL=redis://localhost:6380/0

# LIVE  = fetch from the provider
# REPLAY = feed recorded fixtures through the identical pipeline
DATA_MODE=REPLAY

MARKET_TIMEZONE=Asia/Kolkata
LOG_LEVEL=INFO
